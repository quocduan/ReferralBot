{
  "name": "Referral Bot",
  "description": "A discord bot that allows you to track how many people each member has brought in to the server. It only works on individual servers (unless there's a lot of overlap between servers and even then why would you) so it needs to be hosted individually.",
  "repository": "https://github.com/geekkid1/ReferralBot",
  "env": {
    "BOT_TOKEN":{
      "description": "The token generated by the discord developer portal that links this code to the usable discord bot profile."
    },
    "DEBUG_LOG":{
      "description": "The channel to log debug messages to (currently not working, provide a channel anyway because it breaks otherwise)."
    },
    "INFO_LOG":{
      "description": "Same as debug log but for more verbose messages. Also not working, but also breaks if not provided, so please put a channel here."
    },
    "WEBHOOKS_USERS":{
      "description": "A comma-separated list of discord user IDs, listing the people that are allowed to use the webhook functionality of the bot. Does not include the owner by default so make sure to add your name to the list!"
    }
  },
  "addons":[
    {
      "plan": "heroku-postgresql",
      "options": {
        "version": 12.4
      }
    }
  ]
}
